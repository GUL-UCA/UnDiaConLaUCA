<%= form_for(@guluca) do |f| %>
  <% if @guluca.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@guluca.errors.count, "error") %> prohibited this guluca from being saved:</h2>

      <ul>
      <% @guluca.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :correo %><br>
    <%= f.text_field :correo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
